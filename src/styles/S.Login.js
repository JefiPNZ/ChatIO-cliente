import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    label: {
       color: '#ff5900',
    },
    input: {
        borderWidth: 1,
        borderColor: '#ff5900',
        borderRadius: 4,
        fontSize: 22,
    },
    formContainer: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
        padding: 15,
    },
    inputContainer: {
        width: '100%',
        alignItems: 'stretch',
    },
    title:{
        fontSize: 44,
        color: '#ff5900',
    },
    titleContainer: {
        marginBottom: 50
    },
    buttonContainer: {
        alignItems: 'stretch',
        width: '100%',
    },
    popupInput:{
        borderWidth: 1,
        borderColor: '#ff5900',
        borderRadius: 4,
    },
});