import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    formButton: {
        backgroundColor: '#ff5900',
        borderRadius: 4,
        height: 42,
        alignItems: 'center',
        alignSelf:'center',
        width: '100%',
        marginTop: 15,
    },
    formButtonLabel: {
        color: '#fff',
        fontWeight: "bold",
        fontSize: 30,
    },
    header: {
        fontWeight: 'bold',
        fontSize: 35,
        color: '#ff5900',
        alignSelf:'center',
    },
    paddingView:{
        padding: 15,
    }
});