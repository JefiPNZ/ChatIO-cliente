import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    input: {
        borderWidth: 1,
        borderColor: '#ff5900',
        borderRadius: 4,
        fontSize: 22,
    },
    container: {
        padding: 15,
        flex: 1, 
        alignContent: 'center',
    },
    formContainer: {
        marginTop: 30,
        marginBottom: 30,
    }
});