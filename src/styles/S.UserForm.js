import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    label: {
        fontSize: 16,
    },
    input: {
        borderWidth: 1,
        borderColor: '#ff5900',
        borderRadius: 4,
        fontSize: 22,
    },
    container: {
        alignContent: 'stretch',
    }, 
});